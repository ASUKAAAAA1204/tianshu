# 梁平低空运行基础底座平台 PRD

## 1. 文档控制

- 产品名称：梁平低空运行基础底座平台
- 产品简称：LP-LAOS
- 文档版本：V1.0
- 文档状态：开发基线
- 适用范围：比赛Demo、技术验证和后续单区域试点
- 当前首要目标：在离线环境下完成“数据加载—规则校验—航路计算—运行模拟—异常处置—轨迹审计”闭环
- 首期不做：农业、物流、应急、城管等行业专用业务；自动放飞；法定审批和空管替代；全域实时数据覆盖

后续所有开发、设计、测试和演示内容，默认以本PRD为准。新增需求必须标注为P1/P2，并说明是否影响P0交付。

## 2. 产品定位

平台面向县域低空运行基础管理和辅助决策，不直接控制无人机，不替代国家及地方的法定审批、空管和监管系统。平台负责把空域、规则、设施、飞行器、任务和运行事件统一转换为可计算、可验证、可协同、可追溯的数据与服务。

### 2.1 核心价值

1. 把静态地图升级为带空间、高度、时间和状态的四维空域模型。
2. 把分散的安全要求变成可执行、可解释、可审计的规则。
3. 在硬约束满足后计算主航路和备用航路，而不是只画一条路线。
4. 对运行过程进行模拟、监视和异常处置，形成完整证据链。
5. 为后续不同业务场景提供统一底座和标准接口。

### 2.2 一句话用户承诺

> 输入一个飞行任务，平台能够说明“能不能飞、为什么这样飞、出现问题怎么办、全过程留下什么证据”。

## 3. 用户与使用场景

### 3.1 用户角色

| 角色 | 主要目标 | 核心权限 |
|---|---|---|
| 平台管理员 | 配置用户、数据和规则 | 全部配置和审计权限 |
| 运行管理员 | 创建任务、审核航路、监视运行 | 任务、航路、告警和报告 |
| 运营人员 | 提交飞行任务、查看航路和状态 | 任务创建、结果查看、事件反馈 |
| 安全/飞行人员 | 核验飞行器和运行约束 | 飞行器、航线、事件确认 |
| 评委/演示用户 | 观察系统闭环和结果解释 | 只读演示权限 |

### 3.2 核心使用场景

1. 运行管理员加载梁平验证区域的空域、设施和障碍物数据。
2. 运营人员提交点到点通用飞行任务。
3. 系统根据规则和飞行器能力判断任务是否可计算。
4. 系统生成主航路、备用航路、风险和能耗说明。
5. 运行管理员确认航路并启动模拟运行。
6. 系统接收模拟遥测，展示状态并检测异常。
7. 运行管理员确认重规划建议。
8. 任务完成后查看轨迹、规则版本、告警和报告。

## 4. 产品范围与优先级

### 4.1 P0：比赛必须完成

- 用户登录和角色权限的最小实现；
- 验证区域和图层加载；
- 四维空域网格/节点模型；
- 起降点、备降点、通信和补能设施对象；
- 飞行器能力登记；
- 通用任务创建；
- 硬约束与软目标规则引擎；
- 主航路、备用航路和不可行原因；
- 多任务时间冲突检测；
- 模拟遥测和运行监视；
- 临时管制、偏航、低电量、链路下降事件；
- 告警、人工确认和局部重规划；
- 任务轨迹回放；
- 规则、数据、任务、告警和确认记录审计；
- 自动生成运行报告；
- 离线一键启动和演示备份。

### 4.2 P1：P0稳定后再做

- 真实无人机遥测接入；
- Cesium高质量三维地形；
- 历史数据风险权重校准；
- 起降点服务范围与补能布局分析；
- AI异常模式识别；
- 自然语言查询和报告润色；
- 多用户并发和远程部署。

### 4.3 P2：本次比赛不承诺

- 自动驾驶、自动放飞和自动返航控制；
- 法定空域审批、空管指挥和监管替代；
- 各行业专用业务闭环；
- 全域实时气象、通信和人口数据；
- 面向生产环境的高可用集群和商业计费系统。

## 5. 关键业务流程

```text
数据导入 → 数据校验 → 空域/设施发布 → 任务创建 → 规则预检
→ 航路计算 → 运行人员确认 → 模拟/遥测运行 → 事件检测
→ 告警 → 人工确认 → 局部重规划 → 任务结束 → 回放与报告
```

### 5.1 任务状态机

`DRAFT草稿 → CHECKING校验中 → REJECTED不可行 / ROUTE_READY待确认 → APPROVED已确认 → RUNNING运行中 → PAUSED暂停 → COMPLETED完成 / ABORTED中止 → ARCHIVED归档`

任何状态变化必须记录操作者、时间、原因和关联规则版本。

### 5.2 事件状态机

`DETECTED发现 → CLASSIFIED分级 → SUGGESTED生成建议 → PENDING_CONFIRMATION待人工确认 → ACCEPTED已接受 / REJECTED已拒绝 → RESOLVED已处置 → ARCHIVED已归档`

## 6. 信息架构与页面PRD

### 6.1 页面总览

1. 登录页
2. 运行总览页
3. 空域数据页
4. 设施管理页
5. 飞行器管理页
6. 规则管理页
7. 任务创建与航路计算页
8. 运行监视页
9. 事件与告警页
10. 轨迹回放与报告页
11. 审计日志页

### 6.2 登录页

**目标：**区分管理员、运行管理员、运营人员和演示用户。

**P0功能：**账号登录、演示账号、退出登录、权限不足提示。

**验收：**未登录用户不能访问业务页面；演示账号只能读取和执行预置Demo流程，不能修改正式规则。

### 6.3 运行总览页

**核心区域：**

- 左侧验证区域和图层开关；
- 中央地图；
- 右侧实时运行状态卡；
- 底部任务、告警和数据更新时间。

**必须展示：**

- 可飞/限飞/禁飞/临时管制区域；
- 航路走廊和任务航线；
- 起降点和备降点；
- 飞行器位置和状态；
- 数据来源、版本和时间戳。

**交互：**点击地图对象查看属性；点击告警跳转事件详情；点击任务进入任务详情。

### 6.4 空域数据页

**功能：**导入、预览、校验、发布、停用空间图层和四维空域网格。

**支持格式：**GeoJSON为P0；CSV点位为P0；GeoPackage为P1。

**图层字段：**对象ID、名称、几何、类别、高度范围、时间窗口、状态、来源、版本、可信等级。

**校验规则：**几何有效性、坐标系、时间格式、高度范围、对象ID唯一性和必填字段。

**验收：**非法文件不能发布，并返回具体错误行/字段；发布后生成不可变版本号。

### 6.5 设施管理页

**对象类型：**起降点、备降点、补能点、通信设施、导航设施。

**属性：**位置、容量、服务半径、开放时间、适配机型、当前状态、来源和维护时间。

**功能：**新增、编辑、停用、地图定位、服务范围查看、状态切换。

**验收：**航路计算只能使用状态为“可用”且满足机型条件的设施。

### 6.6 飞行器管理页

**属性：**编号、类型、最大航程、续航、载荷、速度范围、最低通信要求、能量状态、数据来源。

**功能：**登记、编辑、启用/停用、能力校验、查看历史任务。

**验收：**续航、载荷或通信能力不满足任务条件时，规则引擎必须阻止航路发布。

### 6.7 规则管理页

**规则类型：**

- 硬约束：禁飞、限飞、高度、时间、障碍物间隔、通信、续航、设施和冲突；
- 软目标：距离、能耗、风险、时效和通信质量。

**属性：**规则ID、名称、类型、表达式、参数、优先级、生效时间、失效时间、来源、版本、状态。

**功能：**规则草稿、测试、发布、停用、版本对比、影响查询。

**验收：**规则变更必须生成新版本；已归档任务仍引用原规则版本；规则测试可给出通过/失败和原因。

### 6.8 任务创建与航路计算页

**输入字段：**起点、终点、任务时间窗口、飞行器、载荷、最低能量储备、任务优先级、备用航路要求。

**处理步骤：**

1. 校验必填项和坐标；
2. 校验飞行器能力；
3. 校验空域、时间和设施规则；
4. 生成候选航路；
5. 计算距离、预计时间、能耗、风险和通信代价；
6. 输出主航路、备用航路或不可行原因。

**结果展示：**地图路线、航点表、指标卡、风险构成、规则命中、不可行原因和版本信息。

**验收：**同样输入和同样版本数据下结果可复现；不可行时不能只显示“失败”。

### 6.9 运行监视页

**必须展示：**飞行器位置、高度、速度、电量、链路、当前航段、任务状态和预计剩余时间。

**控制：**开始、暂停、继续、结束模拟；注入临时管制、偏航、低电量和链路下降事件；查看处置建议；人工确认重规划。

**边界：**所有控制仅作用于模拟器或平台状态，不向真实飞行器发送控制指令。

### 6.10 事件与告警页

**字段：**事件类型、严重等级、发生时间、位置、证据、影响航段、建议动作、确认人、处置结果。

**等级：**提示、一般、严重、紧急。

**验收：**告警颜色、声音或视觉提示与等级一致；事件必须可关联到任务和轨迹。

### 6.11 轨迹回放与报告页

**功能：**按时间轴回放、显示告警节点、查看人工确认、导出报告。

**报告内容：**任务信息、数据版本、规则版本、计划航路、实际轨迹、告警、处置、重规划前后对比、完成状态和人工确认记录。

**导出：**HTML或PDF为P0；JSON原始记录为P0。

### 6.12 审计日志页

**记录范围：**登录、数据导入、规则发布、任务创建、航路计算、确认、告警处置、报告导出。

**字段：**操作者、角色、动作、对象、旧值、 新值、时间、IP/设备标识、结果和关联任务。

**验收：**关键日志不可由普通用户删除或修改；查询支持按时间、用户、对象和任务过滤。

## 7. 数据模型

### 7.1 AirspaceCell 空域网格

`cell_id, geometry, z_min, z_max, t_start, t_end, status, risk, rule_version, source, confidence, data_version`

### 7.2 AirspaceRule 空域规则

`rule_id, name, type, expression, parameters, priority, effective_from, effective_to, source, version, status`

### 7.3 Facility 基础设施

`facility_id, type, name, location, capacity, service_radius, opening_hours, compatible_vehicles, status, source, data_version`

### 7.4 Vehicle 飞行器

`vehicle_id, name, type, max_range, endurance, payload, speed_min, speed_max, link_requirement, energy_state, status, source`

### 7.5 Mission 任务

`mission_id, origin, destination, time_window, priority, vehicle_id, payload, reserve_requirement, status, creator, created_at`

### 7.6 Route 航路

`route_id, mission_id, corridor_id, waypoints, altitude_band, valid_time, distance, duration, energy, risk, communication_score, total_cost, route_type, rule_version, data_version`

### 7.7 Telemetry 遥测

`telemetry_id, mission_id, vehicle_id, timestamp, longitude, latitude, altitude, speed, heading, energy, link_quality, source`

### 7.8 Event 事件

`event_id, mission_id, type, severity, timestamp, location, evidence, impacted_segment, suggested_action, confirmation, resolution, status`

### 7.9 AuditLog 审计日志

`log_id, actor, role, action, object_type, object_id, before_value, after_value, timestamp, result, correlation_id`

## 8. 规则与算法要求

### 8.1 硬约束优先

硬约束必须在航路搜索前过滤或在搜索过程中禁止违反。至少包括：

- 禁飞区和临时管制区不可穿越；
- 限飞区高度范围不可违反；
- 障碍物安全间隔不可违反；
- 飞行器续航和最低能量储备必须满足；
- 起降点、备降点和通信条件必须满足；
- 多任务航路在同一时间窗口不得发生冲突。

### 8.2 软目标代价

在可行航路中计算：

`C(P)=λd×距离+λe×能耗+λr×风险+λt×时效代价+λc×通信代价`

所有权重和输入版本随结果留档。首期不宣称算法原创；如有算法改进，必须提供基线对照。

### 8.3 风险模型

`R = w1×空域风险 + w2×地面暴露风险 + w3×地形障碍风险 + w4×气象风险 + w5×通信风险`

风险模型输出总风险和分项风险，不允许只显示一个无法解释的分数。没有真实数据的图层必须标记为演示数据。

### 8.4 重规划

重规划只对受影响航段进行局部计算；若局部无可行解，返回全局重规划建议，并要求人工确认。系统不得自动向真实飞行器下发指令。

## 9. 接口要求

### 9.1 P0接口清单

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/auth/login` | 登录 |
| GET | `/api/layers` | 获取图层 |
| POST | `/api/layers/import` | 导入图层 |
| POST | `/api/layers/{id}/publish` | 发布图层版本 |
| GET/POST | `/api/facilities` | 查询/新增设施 |
| GET/POST | `/api/vehicles` | 查询/登记飞行器 |
| GET/POST | `/api/rules` | 查询/创建规则 |
| POST | `/api/rules/test` | 测试规则 |
| POST | `/api/missions/check` | 任务预检 |
| POST | `/api/routes/compute` | 计算航路 |
| POST | `/api/routes/{id}/approve` | 人工确认航路 |
| POST | `/api/simulations/start` | 启动模拟 |
| POST | `/api/simulations/{id}/event` | 注入事件 |
| POST | `/api/events/{id}/confirm` | 确认处置建议 |
| GET | `/api/missions/{id}/replay` | 获取回放数据 |
| GET | `/api/missions/{id}/report` | 生成报告 |
| GET | `/api/audit-logs` | 查询审计日志 |

### 9.2 接口通用要求

- JSON编码UTF-8；
- 时间统一使用ISO 8601；
- 坐标统一记录坐标系；
- 返回`code, message, data, request_id`；
- 错误必须包含可读原因和可定位字段；
- 所有写操作带操作者和幂等请求ID。

## 10. 非功能需求

### 10.1 性能

- Demo数据规模下，页面首次加载目标不超过5秒；
- 单任务候选航路计算目标不超过5秒；
- 事件注入到告警显示目标不超过2秒；
- 轨迹回放支持至少10000个轨迹点。

以上均为目标值，最终以测试环境和数据规模记录为准。

### 10.2 可用性

- 支持Windows本地启动；
- 支持无网络运行；
- 支持一键加载演示数据；
- 关键页面在1366×768分辨率下可用；
- 重要操作有成功、失败和处理中状态。

### 10.3 安全与合规

- 最小权限；
- 敏感数据不写入前端日志；
- 关键配置变更留痕；
- 不接入真实控制链路；
- 明确数据来源、许可和演示数据标识。

### 10.4 可维护性

- 配置与代码分离；
- 规则、数据和算法版本可回滚；
- 核心算法有单元测试；
- 统一错误码和日志格式；
- 关键模块有README和启动说明。

## 11. 验收测试

### 11.1 功能验收

1. 能导入并发布合法图层。
2. 能拒绝非法图层并指出错误。
3. 能创建飞行器和通用任务。
4. 能阻止违反硬约束的航路。
5. 能生成主航路和备用航路。
6. 能说明航路不可行原因。
7. 能检测两个任务的时间冲突。
8. 能启动模拟并显示遥测。
9. 能注入四类异常并显示告警。
10. 能人工确认重规划建议。
11. 能完成轨迹回放。
12. 能生成HTML/PDF和JSON报告。
13. 能查询关键审计日志。
14. 演示账号不能修改正式规则和数据版本。

### 11.2 算法验收

- 同输入、同版本数据、同参数时结果可复现；
- 禁飞区内不得出现航点；
- 航路高度不得超出机型和区域限制；
- 能量不足时任务被拒绝或要求更换飞行器/航路；
- 临时管制后受影响航段被识别；
- 重规划结果仍通过全部硬约束；
- 每次计算可追溯到规则和数据版本。

### 11.3 演示验收

完整流程在5分钟内完成；连续演示3次至少成功2次；网络断开后仍可使用预置数据完成演示；若程序异常，可切换录屏、截图和结果JSON，不影响路演表达。

## 12. 开发计划

### Sprint 0：项目骨架（1天）

- 初始化前后端、目录、环境和启动脚本；
- 建立示例数据、错误码和README；
- 完成登录和基础布局。

### Sprint 1：数据与地图（2—3天）

- 图层导入、校验、发布；
- 地图展示和图层开关；
- 空域网格、设施和飞行器对象。

### Sprint 2：规则与航路（3—5天）

- 规则数据结构和测试；
- 硬约束校验；
- 四维网格/图搜索；
- 主备航路、风险和代价结果。

### Sprint 3：运行模拟（2—3天）

- 模拟遥测；
- 运行监视；
- 冲突和异常事件；
- 告警与人工确认。

### Sprint 4：审计与报告（2天）

- 轨迹回放；
- 报告导出；
- 审计日志；
- 版本关联。

### Sprint 5：比赛封装（2—3天）

- 离线部署；
- 演示数据固化；
- 录屏和截图；
- 5分钟脚本；
- 验收测试和故障预案。

## 13. 两人开发分工

### 成员A：产品、业务与验收

- 维护需求和范围；
- 建立梁平数据来源清单；
- 定义空域、设施、规则和任务字段；
- 编写规则案例和验收用例；
- 负责PPT、演示脚本、访谈证据和答辩；
- 每日进行产品验收和风险记录。

### 成员B：技术、算法与交付

- 搭建前后端和数据模型；
- 实现地图、规则、航路和模拟器；
- 实现接口、日志和报告；
- 编写单元测试和部署脚本；
- 负责现场技术保障和故障切换。

### 共同责任

- 每日同步一次；
- 每个Sprint结束必须有可运行版本；
- 任何新增需求先判断是否破坏P0；
- 两人都能独立完成核心Demo。

## 14. 开发规范

- 分支或版本命名：`v0.x-feature`、`v1.0-demo`；
- 数据目录分为`source`、`processed`、`demo`，不得混用；
- 规则、数据和算法配置必须带版本号；
- 提交代码前运行格式检查和最小测试；
- 禁止把真实敏感数据直接提交到公开仓库；
- 每次演示前锁定数据和代码版本；
- 对外文案不得把目标值写成已验证结果。

## 15. 风险处理原则

1. 数据不足：使用明确标注的演示数据，不虚构真实结果。
2. 三维开发延期：先交付二维/2.5D可计算地图，三维仅作视觉增强。
3. 算法不稳定：回退到确定性规则和基础栅格搜索。
4. 真实设备不可用：使用模拟遥测完成闭环，不宣称真实飞行验证。
5. 网络或设备故障：切换离线包、录屏、截图和JSON结果。
6. 需求膨胀：冻结P0，新增内容转入P1/P2。

## 16. 完成定义（Definition of Done）

一个功能只有同时满足以下条件才算完成：

1. 页面或接口可运行；
2. 正常路径和失败路径均有处理；
3. 关键操作有日志；
4. 至少有一个自动化或手工验收用例；
5. 数据、规则和算法版本可追溯；
6. 成员A能从产品角度讲清楚；
7. 成员B能从技术角度解释实现；
8. 不违反本PRD的范围和安全边界。

## 17. 最终成功标准

比赛版本达到以下状态即视为成功：

- 评委可以看到梁平验证区域的空域和设施数据；
- 评委可以输入任务并看到规则校验结果；
- 系统可以计算主/备航路或解释为什么不可行；
- 系统可以模拟运行并注入异常；
- 系统可以由人工确认重规划；
- 系统可以回放和导出全过程证据；
- 两人团队能够在5分钟内稳定完成Demo；
- 所有结果都能说明数据、规则和版本来源。

