# 首届“飞阅中国”低空经济应用场景大赛参赛方案

## 一、项目基本信息

- 参赛赛道：低空基础设施与综合服务
- 项目名称：梁平县域数字空域与低空运行智能服务平台
- 项目简称：梁平低空运行基础底座（LP-LAOS）
- 项目定位：面向梁平区低空飞行活动，建设集“四维空域模型、规则引擎、安全航路、运行监视、数据审计”于一体的县域低空运行基础底座。
- 核心命题：将县域低空飞行变成可计算、可验证、可协同、可追溯的基础能力。
- 参赛形式：PPT路演答辩 + 平台Demo + 三维效果图/演示视频。

## 二、项目摘要

随着县域低空飞行由零散作业向多主体、多机型、多任务并发发展，传统依赖人工报备、分散通信和单任务管理的方式，难以支撑规模化、安全化、常态化运行。当前最需要优先建设的不是某一个行业应用，而是能够统一表达空域、规则、设施、飞行器和任务的运行基础底座。

本项目拟构建梁平县域数字空域与低空运行智能服务平台。平台以GIS和三维空间数据底座为基础，汇聚地形地貌、建筑物、地面敏感目标、气象、通信、起降点、空域规则、无人机及任务等数据，形成带有时间维度、可动态更新的四维低空运行模型；通过规则引擎、安全航路规划、多任务协同调度、运行监视、异常预警和数据审计，实现飞行任务从计划、校验、执行、监控到归档回放的全生命周期闭环。只有当模型与实时状态持续同步时，才将其称为数字孪生展示，不以静态三维地图冒充数字孪生。

项目采取“小切口验证、基础底座先行、场景插件扩展”的落地策略，初期选择一个可核验区域、一个航路走廊和一组通用任务完成最小闭环，后续再通过标准接口接入不同业务场景。平台不替代法定审批和监管系统，而是作为跨部门、跨运营主体的辅助决策和运行服务基础设施。

## 三、需求与痛点

### 3.1 管理侧痛点

1. 空域、地理、气象、通信、设施和任务数据分散，缺乏统一的四维低空数字底图。
2. 飞行申请、航线设计、安全核验和任务归档链条割裂，协同效率低。
3. 多架无人机或多个运营主体同时作业时，缺乏统一态势感知与冲突预警。
4. 临时管制、天气变化或运行异常下，任务下达、航路调整和多主体协同依赖人工经验。
5. 飞行数据难以沉淀为可审计、可评价、可复用的数据资产。

### 3.2 运营侧痛点

1. 每次任务重复踏勘和规划，执行成本高。
2. 不同机型的续航、载荷、抗风和通信能力差异大，固定航线并不安全。
3. 起降点、备用降落点、补能点和通信覆盖信息不透明，限制常态化运行。
4. 缺少统一数据标准和接口，不同运营主体难以共享航路、设施和运行数据。

### 3.3 项目要回答的核心问题

- 在给定任务、机型、天气和空域约束下，哪条航路安全且成本最低？
- 多任务并发时，如何调度无人机、起降点和时间窗口？
- 飞行过程中出现偏航、失联、低电量或临时风险时，如何及时预警和处置？
- 如何把一次飞行转化为可复盘、可监管、可计费、可优化的数据闭环？

## 四、总体方案

### 4.1 总体架构

平台采用“六层一体系”架构：

1. 感知接入层：无人机、机载载荷、北斗/GPS、遥测链路、气象设备、视频和第三方业务系统。
2. 四维空域模型层：将空间位置、高度、时间窗口和运行状态统一表达。
3. 数据底座层：地形、建筑、道路、水系、地面敏感目标、空域规则、气象、通信、起降点、备降点、补能设施、飞行器和历史任务数据。
4. 规则与智能引擎层：硬约束校验、风险计算、航路规划、冲突检测、任务调度和异常识别。
5. 业务服务层：计划编排、航路审核辅助、实时监控、告警处置、数据归档和运行分析。
6. 应用展示层：管理驾驶舱、运营工作台、移动端和标准开放接口。

安全保障体系贯穿上述六层：身份权限、数据分级、操作留痕、链路加密、容灾备份和人工接管。

### 4.2 四项核心能力

#### 模块一：四维可计算空域模型

- 三维展示地形、建筑、起降点、航路、任务和无人机状态，并增加时间窗口和临时状态。
- 将区域划分为可飞、限飞、禁飞、风险和临时管控网格。
- 支持气象、通信覆盖及地面敏感目标等图层叠加。
- 对每个网格形成动态风险标签，为规则校验、规划和调度提供约束。
- 将起降点、备降点、补能点、通信和导航设施作为具有位置、容量、服务范围、开放时间和运行状态的基础设施对象进行管理。

空域模型不是静态地图，而是`(x, y, z, t)`四维对象：同一空间位置在不同时间可能具有不同的管制、气象和运行状态。

#### 模块二：规则引擎与安全航路计算

- 先校验空域、时间、机型、设施和安全规则，再生成可行航路。
- 在满足硬约束的候选结果中，综合距离、能耗、地形暴露、通信质量、天气和任务时效进行排序。
- 输出推荐航线、备降点、能耗估计、风险等级和备用航线。
- 当出现临时管制、天气变化或通信异常时，触发动态重规划。

规则分为两类：硬约束用于排除不可行航路，软目标用于优化可行航路。每条规则均记录版本、生效时间、来源和影响结果，保证拒绝或推荐一条航路时可解释、可审计。

平台将“航路走廊”和“任务航线”分层：航路走廊是经过基础安全校验的可复用通道，任务航线则根据具体飞行器、时间窗口和任务要求在走廊内动态生成。

#### 模块三：运行监视与多主体协同

- 实时显示位置、高度、速度、电量、链路和任务状态。
- 对航线交叉、时间冲突、异常偏航、失联和低电量进行分级告警。
- 根据任务优先级、机型能力、起降点容量和时间窗口自动派单。
- 为高优先级任务预留时间窗口，并支持人工确认和接管。

#### 模块四：运行数据审计与反哺

- 建立飞行器、人员、航线、任务、告警和成果数据标准。
- 自动形成任务台账、轨迹档案、事件记录和统计报告。
- 提供标准API，为后续不同业务系统调用统一的空域、航路和运行服务。
- 对历史任务进行分析，为航路、起降点和补能设施布局提供决策依据。

## 五、关键算法设计

### 5.1 网格化风险模型

将低空区域划分为带高度和时间属性的四维网格，对每个网格计算综合风险值：

`R = w1×空域风险 + w2×地面暴露风险 + w3×地形障碍风险 + w4×气象风险 + w5×通信风险`

风险计算服从规则引擎的硬约束。权重不由AI直接决定，而是通过专家打分、历史任务和验证结果校准。平台保留各项风险来源，保证结果可解释。

在所有硬约束均满足的前提下，对候选航线计算综合代价：

`C(P) = λd×距离 + λe×能耗 + λr×风险 + λt×时效代价 + λc×通信代价`

权重、数据版本和计算结果随航线一并留档，便于复核和对照实验。

### 5.2 多约束航路规划

先利用规则引擎剔除不满足空域规则、安全间隔、机型性能和基础设施条件的区域，再使用栅格搜索生成可行航线，并根据距离、能耗、风险和任务时效进行多目标排序。动态场景下使用增量式搜索进行局部重规划。若后续对搜索策略作出实质改进，将通过基线对照实验单独证明，不在报名阶段夸大算法原创性。

### 5.3 多任务协同调度

将飞行器能力、任务优先级、运行窗口、起降点容量和补能需求转化为约束优化问题，输出“任务—飞行器—航路—时间窗口”匹配结果。高优先级任务可以抢占普通任务的候选窗口，但必须经过规则校验和授权确认。

### 5.4 运行异常检测与后续AI增强

- 首期采用可解释规则检测偏航、速度突变、电量异常、链路质量下降和越界运行。
- 后续再利用历史运行数据训练异常模式识别模型，提升对复合风险的提前提示能力。
- 文本智能可将告警、轨迹和处置记录汇总为运行报告，但不改变规则判断结果。
- 使用边界：AI只做风险提示和辅助决策，审批、放飞和重大处置由授权人员确认。

## 六、首期验证设计

### 6.1 通用验证单元

首期不绑定农业、物流、应急或城管等具体行业，而是选择一个可核验区域、一条航路走廊、若干起降/备降点和一组通用任务，完成端到端闭环。具体地点须经梁平当地主管部门、运营主体和现场踏勘确认，不在报名阶段虚构点位。

### 6.2 验证对象

验证对象包括：空域规则加载、航路走廊生成、任务航线计算、多任务时间冲突、动态风险变化、运行状态接入、告警处置和轨迹审计。后续行业应用通过标准接口接入，不改变底座核心模型。

### 6.3 Demo演示流程

1. 在梁平三维地图选择起点、终点和通用任务类型。
2. 录入飞行器性能、载荷、电量和任务时间窗口。
3. 系统展示禁限飞约束和风险热力图。
4. 自动生成主航线、备用航线、预计时间和能耗。
5. 启动模拟飞行，展示实时位置和状态。
6. 注入“临时风险区”或“链路下降”事件。
7. 系统告警并生成重规划建议，由人员确认执行。
8. 任务完成后自动生成轨迹、告警、处置和绩效报告。

## 七、项目创新点

1. 建立空间、时间、规则和运行状态统一表达的四维可计算空域模型。
2. 将规则硬约束与软目标优化分层，明确给出航路不可行原因和推荐依据。
3. 将航路走廊与任务航线解耦，支持不同飞行器和后续业务以插件方式接入。
4. 采用“规则校验 + 算法优化 + 人工确认”的可信运行机制，保证结果可解释、可审计。
5. 将飞行数据沉淀为空域、航路、设施和运行参数的持续校准依据。
6. 使用模块化、标准化接口，避免不同场景重复建设底层能力。

## 八、可实施性与安全设计

### 8.1 系统边界

平台属于辅助规划、运行服务和态势感知系统，不替代国家及地方现行的法定审批、空管和监管系统。所有真实飞行均以有效法规、空域批复、人员资质和设备合规为前提。

首期明确适用运行边界：仅对数据来源可核验、空域规则已加载、飞行器能力已登记、通信与定位条件满足要求的验证区域和任务提供辅助建议。超出数据覆盖、设备能力或规则有效范围时，平台必须提示“不可计算/需人工复核”，不得输出无依据的自动放行结论。

### 8.2 三级安全机制

- 事前：人员、设备、空域、气象、航线和任务规则校验。
- 事中：电子围栏、冲突检测、偏航/失联/低电量告警、人工接管。
- 事后：全量留痕、事件复盘、责任链记录和模型参数更新。

### 8.3 技术实现建议

- 前端：WebGIS三维可视化和运营驾驶舱。
- 后端：任务管理、规则引擎、航路规划、调度和告警服务。
- 数据：空间数据库 + 时序数据库 + 对象存储。
- 算法：四维风险网格、规则约束搜索、约束调度、可解释异常检测。
- 接入：先用模拟器完成验证，再接入一款真实无人机进行小范围试飞。

## 九、实施计划

### 第一阶段：基础模型与概念验证（0—2周）

- 完成报名方案、项目PPT和团队分工。
- 建立梁平基础空间数据Demo，跑通数据加载、规则校验、任务创建、航线生成和模拟飞行。
- 确定首期验证区域、航路走廊和待调研清单。

### 第二阶段：规则与航路最小闭环（第3—6周）

- 获取并核验验证区域、起降/备降点、空域规则和运行需求数据。
- 完成风险图层、规则引擎、航路走廊、任务航线、冲突告警和轨迹回放。
- 访谈至少1名飞手、1家运营主体、1个潜在使用部门或单位。
- 使用真实机型参数修正规则和能耗模型。

### 第三阶段：验证与决赛展示（第7—10周）

- 完成仿真测试和条件允许范围内的小规模试飞验证。
- 建立测试指标、异常案例和人工接管记录。
- 完成三维演示视频、决赛PPT、答辩题库和项目实施预算。

### 第四阶段：赛后底座扩展（3—12个月）

- 扩大验证区域和航路走廊覆盖。
- 接入更多运营主体、飞行器和业务系统。
- 根据运行数据完善起降点、备降点、补能和通信设施规划。
- 在底座稳定后，通过标准接口接入不同业务应用，不改变核心模型。

## 十、验证指标

报名版不承诺未经测试的高精度指标。团队应通过Demo、数据回放和条件允许范围内的试飞形成真实基线，并重点记录：

- 数据图层的来源、版本、时间戳和覆盖范围完整性；
- 硬约束违规率和不可行航线原因的正确性；
- 任务建单至生成候选航线的耗时；
- 已知航路冲突的检出率和告警延迟；
- 临时限制、低电量或链路下降注入后的重规划耗时；
- 飞行状态接入延迟及轨迹完整率；
- 告警、人工确认和处置记录完整率；
- 相比人工流程节省的规划时间；
- 运行人员对结果可解释性和操作性的评价。

## 十一、商业模式与产业价值

### 11.1 建设与运营模式

建议采用“政府指导、平台企业建设、运营主体使用、业务系统按接口或运行服务调用”的模式。根据实际合作条件，可采用平台建设费、年度技术服务费、运行服务费和标准接口服务费组合，不在缺乏调研时虚构收益规模。

### 11.2 价值体现

- 管理价值：提升低空态势感知和跨部门协同效率。
- 安全价值：将风险识别前移，形成事前、事中、事后闭环。
- 运营价值：减少重复踏勘和航线规划，提高无人机及起降设施利用率。
- 产业价值：形成面向后续各类场景的公共数字底座，降低重复建设和企业接入成本。
- 数据价值：为后续航路网、起降点、补能和通信设施布局提供证据。

## 十二、团队分工与人工壁垒

1. 项目负责人：需求整合、政企沟通、进度和路演。
2. 低空业务/飞手：机型参数、安全规则、航线和异常处置校验。
3. 算法工程师：风险建模、航路规划、调度和异常检测。
4. 全栈/GIS工程师：数据底座、接口、三维地图和平台Demo。
5. 产品与调研：用户访谈、流程设计、成本测算和证据整理。

真正壁垒不是AI生成的文字，而是以下可验证资产：

- 经核验的梁平试点区域数据和风险图层；
- 飞手、运营主体和管理人员共同确认的规则库；
- 一条实际可验证的示范航线；
- 真实机型能耗、链路和异常测试数据；
- 完整的任务—轨迹—告警—处置—报告闭环；
- 主管部门、运营企业或应用单位的访谈、意向或联合验证材料。

## 十三、建议预算框架

正式预算须询价后确定。报名阶段按科目展示，避免虚构精确金额：

- 软件研发：GIS、后端、算法、接口和可视化；
- 数据建设：地图处理、现场踏勘、图层制作和数据治理；
- 设备接入：无人机或模拟器、定位、通信及测试设备；
- 测试验证：场地、保险、安全保障和第三方技术支持；
- 运维保障：云资源、备份、安全、培训和售后；
- 预备费用：应对数据接口、设备适配和现场条件变化。

决赛前至少给出三个预算层级：Demo验证版、单区域试点版、县域推广版，并附询价或测算依据。

## 十四、10分钟路演结构

1. 0:00—0:45：真实问题——县域低空规模化后为何需要统一运行底座。
2. 0:45—1:30：梁平基础条件与通用验证区域。
3. 1:30—3:00：四维空域、规则引擎与安全航路主线。
4. 3:00—5:30：现场Demo，完成规划、监控、异常和报告闭环。
5. 5:30—6:30：规则约束、航路计算和可信AI边界。
6. 6:30—7:30：安全合规与人工接管机制。
7. 7:30—8:30：实施路径、团队和合作资源。
8. 8:30—9:30：运营模式与产业价值。
9. 9:30—10:00：总结——从一个通用验证单元形成县域低空公共数字底座。

## 十五、报名阶段必须补齐的材料

- 团队名称、成员、单位和联系方式；
- 每位成员的专业能力及相关成果；
- 现有软件、算法、专利、论文、样机或合作资源；
- 梁平首期验证区域和航路走廊的选择依据；
- 至少一份潜在用户或飞手访谈纪要；
- Demo首页或架构效果图；
- 所有本地数据、法规和产业数据的来源及日期；
- 不确定数据统一标记为“待主管部门/现场踏勘核验”。

## 十六、一句话路演口号

**让梁平每一次低空飞行，都可计算、可验证、可协同、可追溯。**
