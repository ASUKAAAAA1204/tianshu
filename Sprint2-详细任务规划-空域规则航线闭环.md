# Sprint 2 详细任务规划

## 1. 迭代定位

本轮目标：把系统从“数据底座 + 静态展示”推进到“可创建任务、可检查空域规则、可生成候选航线、可阻断违规任务”的首个业务闭环。

本轮不做农业、应急、物流等行业适配；不做自动飞行控制；不依赖外部地图服务；不追求复杂三维效果。

核心演示链路：

```text
打开平台 → 查看空域图层 → 新建任务 → 选择起终点和高度
→ 执行规则检查 → 返回风险结果 → 合规则生成航线
→ 违规则阻断并说明原因 → 保存任务和审计记录
```

## 2. 本轮完成定义

Sprint 2 完成后，评委可以在浏览器中完成以下动作：

1. 查看梁平演示区域的真实坐标空域图层。
2. 打开或关闭图层，并查看图层名称、类型、等级和版本。
3. 创建一条任务，输入起点、终点、飞行高度和飞行器。
4. 点击“规则检查”，得到硬约束、软约束和风险摘要。
5. 对合规任务生成一条基础候选航线。
6. 对穿越限制区或高度超限的任务阻断提交，并展示明确原因。
7. 查看任务详情、规则检查结果和审计记录。
8. 关闭网络后仍可重复上述演示。

## 3. 任务优先级

| 优先级 | 范围 | 本轮要求 |
|---|---|---|
| P0 | 图层管理、任务详情、规则检查、基础航线、审计 | 必须完成 |
| P1 | 图层导入、图层版本切换、备选航线、风险评分 | 有余力完成 |
| P2 | Cesium 三维、真实地图底图、复杂路径算法 | 本轮不做 |

## 4. 工作分解

### 4.1 A 同学：后端、数据和规则引擎

#### A-01 数据库扩展

- 增加 `layer_versions` 或在 `layers` 表中完善版本字段。
- 增加 `mission_checks`：任务规则检查结果。
- 增加 `routes`：候选航线节点、距离、预计耗时、风险等级。
- 增加 `route_points` 或使用 JSON 保存点序列。
- 增加必要索引：任务编号、图层状态、规则编码、创建时间。
- 编写数据库迁移/初始化兼容逻辑，不能破坏现有演示数据库。

交付物：数据库表结构、初始化种子、迁移说明、数据字典更新。

#### A-02 图层接口

- `GET /api/layers`：返回图层元数据和 GeoJSON 几何。
- `POST /api/layers/import`：接收 GeoJSON，执行格式校验。
- `POST /api/layers/{id}/publish`：发布指定版本。
- `POST /api/layers/{id}/disable`：停用图层。
- 校验几何类型、坐标数量、经纬度范围、必填属性。
- 错误返回包含字段、错误码和修复建议。

#### A-03 规则检查服务

实现纯 Python 可测试服务，不耦合 HTTP：

- 高度上限检查。
- 任务线段与硬限制区相交检查。
- 任务线段与临时限制区相交检查。
- 软规则提示。
- 结果分为 `pass`、`warning`、`blocked`。
- 每条结果包含规则编码、原因、空间对象、建议动作。

建议接口：

```http
POST /api/missions/{mission_id}/check
```

响应至少包含：

```json
{
  "decision": "blocked",
  "risk_level": "high",
  "items": [
    {
      "rule_code": "R-001",
      "level": "hard",
      "message": "航线穿越梁平机场净空保护区",
      "action": "调整起终点或航线"
    }
  ]
}
```

#### A-04 基础航线服务

- 对合规任务生成起点到终点的直连航线。
- 对穿越限制区的任务，使用规则检查结果阻断直连航线。
- P1 可增加 3×3 或 5×5 网格绕行。
- 计算总距离、预计耗时、最高高度和风险等级。
- 保存航线和航线点序列。

建议接口：

```http
POST /api/missions/{mission_id}/routes/plan
GET  /api/missions/{mission_id}/routes
```

#### A-05 任务状态和审计

- 创建后状态为 `planned`。
- 规则检查后记录检查结果。
- 航线生成后记录候选航线。
- 重要动作统一写入 `audit_logs`。
- 不允许通过接口直接跳过必要状态。

#### A-06 后端测试

至少增加：

- 合法 GeoJSON 通过测试。
- 空几何、非法坐标、缺少属性被拒绝。
- 高度超限被阻断。
- 穿越硬限制区被阻断。
- 进入软限制区返回 warning。
- 合规任务可生成路线。
- 重复任务编号返回 409。

### 4.2 B 同学：前端、交互和演示流程

#### B-01 图层管理面板

- 显示图层列表：名称、类型、限制等级、状态、版本。
- 支持图层开关。
- 地图实时反映开关结果。
- 支持“导入图层”入口，显示校验结果。
- 对演示数据标记“演示数据”。

#### B-02 任务创建体验

- 将当前弹窗升级为分步表单：
  1. 基本信息
  2. 起终点与高度
  3. 飞行器选择
  4. 规则检查
- 经纬度输入增加范围提示。
- 高度输入增加 20—1200 米提示。
- 表单错误显示在字段附近，不只显示全局提示。

#### B-03 任务详情页

显示：

- 任务基本信息。
- 关联飞行器。
- 起点、终点、高度。
- 规则检查结果。
- 候选航线信息。
- 当前任务状态。
- 审计时间线。

#### B-04 规则检查结果交互

- 增加“执行规则检查”按钮。
- 使用颜色区分：通过、警告、阻断。
- 阻断结果必须展示规则编码和具体原因。
- 软约束允许“人工确认并继续”按钮，但必须写审计日志。
- 硬约束不提供绕过按钮。

#### B-05 航线展示

- 地图绘制航线点和线段。
- 展示距离、预计耗时、风险等级。
- 展示主航线和备选航线卡片。
- 没有可行路线时显示原因和修改建议。

#### B-06 前端测试与演示准备

- 新建合法任务流程测试。
- 高度错误提示测试。
- 阻断状态展示测试。
- 离线刷新测试。
- 准备 3 组固定演示任务：合法、硬约束阻断、软约束警告。

## 5. 推荐接口清单

| 方法 | 路径 | 作用 | 优先级 |
|---|---|---|---|
| GET | `/api/layers` | 图层列表 | P0 |
| POST | `/api/layers/import` | 导入并校验 GeoJSON | P0 |
| POST | `/api/layers/{id}/publish` | 发布图层 | P0 |
| POST | `/api/layers/{id}/disable` | 停用图层 | P1 |
| GET | `/api/missions/{id}` | 任务详情 | P0 |
| POST | `/api/missions/{id}/check` | 执行规则检查 | P0 |
| POST | `/api/missions/{id}/routes/plan` | 生成航线 | P0 |
| GET | `/api/missions/{id}/routes` | 获取候选航线 | P0 |
| POST | `/api/missions/{id}/confirm` | 人工确认软约束 | P1 |
| GET | `/api/audit-logs` | 审计记录 | P0 |

## 6. 计划节奏

### 第 1 天：接口和数据冻结

- A：完成数据库表设计、接口请求响应样例。
- B：完成任务详情和图层管理页面线框。
- 双方：冻结字段名、状态枚举和错误码。

日末产物：`schema.md`、接口契约、页面线框、演示数据三组样例。

### 第 2 天：图层管理

- A：实现 GeoJSON 校验、导入和发布接口。
- B：实现图层列表、开关和导入结果展示。

日末验收：合法图层可导入，非法图层被拒绝，地图可隐藏/显示图层。

### 第 3 天：规则引擎

- A：实现高度、相交、软硬约束判定。
- B：接入规则检查按钮和结果面板。

日末验收：三组演示任务返回预期结果。

### 第 4 天：基础航线

- A：实现直连航线、距离、耗时和保存。
- B：绘制航线，完成主航线卡片和详情展示。

日末验收：合法任务可以生成并查看航线，阻断任务无法生成航线。

### 第 5 天：审计、联调和测试

- A：补全状态流转、审计和后端测试。
- B：补全错误状态、加载状态和前端演示路径。
- 双方：共同完成接口联调。

日末产物：完整可演示闭环、测试报告、问题清单。

### 第 6 天：稳定性和答辩演示

- 连续执行 10 次演示。
- 断网启动和刷新测试。
- 清空并重建演示数据库测试。
- 录制 3 分钟核心演示视频。
- 固定答辩数据和讲解顺序。

## 7. 验收标准

### 功能验收

- [ ] 能查看并切换至少 4 个空域图层。
- [ ] 能导入合法 GeoJSON。
- [ ] 能拒绝坐标非法或几何为空的 GeoJSON。
- [ ] 能创建任务并持久化。
- [ ] 能执行规则检查并保存结果。
- [ ] 高度超过 1200 米时必须阻断。
- [ ] 航线穿越硬限制区时必须阻断。
- [ ] 软限制区至少返回 warning。
- [ ] 合规任务能生成航线并显示距离、耗时。
- [ ] 检查、导入、发布、确认动作有审计记录。

### 质量验收

- [ ] 后端自动化测试不少于 10 条。
- [ ] 关键 API 错误响应结构统一。
- [ ] 浏览器刷新后数据仍存在。
- [ ] 服务重启后数据仍存在。
- [ ] 离线环境可完成三组演示。
- [ ] 连续 10 次演示没有阻断性故障。
- [ ] 前端不出现未捕获异常。

## 8. 风险与降级方案

| 风险 | 触发条件 | 降级处理 |
|---|---|---|
| 空间计算库安装失败 | Shapely 无法安装 | 使用矩形包围盒相交算法，明确标注演示精度 |
| 地图组件接入延迟 | 第 3 天仍未完成 | 保留 SVG 坐标图，优先保证闭环 |
| 航线算法复杂 | 直连路线无法绕行 | 本轮只做“可行直连/阻断”，绕行列入 P1 |
| 前后端字段不一致 | 联调出现 400/422 | 以接口契约为准，禁止前端自行猜字段 |
| 演示数据损坏 | 启动时种子加载失败 | 提供数据库重建脚本和只读备份 |
| 时间不足 | 第 4 天 P0 未完成 | 砍掉导入 UI、备选路线和动画，保留规则闭环 |

## 9. 本轮禁止事项

- 不引入农业、应急、物流等业务流程。
- 不实现自动飞行控制或自动审批。
- 不接入未经验证的外部实时数据服务作为唯一数据源。
- 不为了视觉效果重写已通过测试的后端接口。
- 不把固定演示状态伪装成真实实时运行结果。

## 10. Sprint 2 结束后的下一步

若本轮验收全部通过，Sprint 3 进入“模拟飞行与动态事件处置”：

```text
启动任务 → 遥测模拟 → 偏航/低电量/临时限制区
→ 告警 → 备选航线 → 人工确认 → 继续执行 → 回放审计
```

若规则闭环未通过，下一轮不得扩展三维、AI 或行业功能，优先修复数据、规则和状态流转问题。
