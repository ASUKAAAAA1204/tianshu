# 梁平低空运行基础底座商业化开发方针

## 1. 总方针

项目从“比赛型 MVP”转向“可封闭试点的低空运行基础平台”，坚持以下顺序：

```text
安全与权限 → 数据与空间底座 → 运行可靠性 → 真实设备适配 → 区域化商业交付 → 行业扩展
```

在完成安全、数据、审计和人工确认之前，不开发自动飞行控制，不把模拟数据包装成真实运行能力，不先做农业、应急、物流等行业特例。

## 2. 产品定位边界

### 当前定位

面向县域、园区和低空运营组织的：

- 数字空域数据底座
- 低空任务编排平台
- 规则检查与风险提示工具
- 飞行态势和事件处置平台
- 运行审计与回放平台

### 明确不替代

- 空域审批系统
- 空管指挥系统
- 飞行许可系统
- 无人机厂商飞控系统
- 应急指挥系统
- 监管执法系统

所有涉及真实飞行的动作必须保留人工确认和责任主体。

## 3. 商业化目标分级

### M0：竞赛演示版（当前）

- 离线可运行。
- 演示数据可重置。
- 支持任务、规则、航线、模拟飞行和事件闭环。
- 不接入真实无人机。

### M1：封闭试点版

目标：单组织、单区域、内网环境下供运营人员试用。

必须满足：

- 登录与角色权限。
- PostgreSQL/PostGIS数据底座。
- 严格多边形空间判断。
- 规则版本和数据来源管理。
- 数据备份和恢复。
- 统一日志、审计和错误追踪。
- Docker部署和一键初始化。
- 真实地图底图或可替换地图适配器。
- 所有真实操作保留人工确认。

### M2：区域平台版

目标：多个组织、多个区域、多个设备接入。

必须满足：

- 多租户和组织隔离。
- 设备适配器和标准遥测模型。
- WebSocket或消息队列实时接入。
- 告警通知渠道。
- 动态重规划和事件回放。
- 细粒度权限和审批流。
- 高可用与故障恢复。

### M3：正式商业交付版

目标：可签约、可部署、可运维、可持续升级。

必须满足：

- 安全测评和等保准备。
- SLA、备份、灾备和回滚。
- 多环境发布和灰度升级。
- 压力测试和容量模型。
- 用户培训、操作手册和运维手册。
- 厂商设备兼容认证。

## 4. Sprint 4—6路线

### Sprint 4：安全、权限和生产数据底座

优先级：最高。

#### P0

- 用户登录、注销和会话管理。
- 角色：管理员、调度员、观察员、审计员。
- API权限中间件。
- 敏感操作二次确认。
- PostgreSQL/PostGIS适配层，保留SQLite离线模式。
- 数据库迁移机制。
- 规则、图层、任务、事件审计关联用户。
- 统一请求ID和结构化日志。

#### 验收门槛

- 未登录不能访问业务接口。
- 观察员不能导入、发布或停用图层。
- 调度员不能重置数据库。
- 审计员只能查看审计记录。
- SQLite演示模式和PostGIS试点模式使用同一业务接口。

### Sprint 5：严格空间能力与试点部署

优先级：高。

#### P0

- Shapely/GEOS或PostGIS空间相交。
- 多边形、孔洞、缓冲区和边界容差处理。
- 高度、时间窗口和图层版本参与规则判断。
- Docker Compose部署。
- 数据库备份、恢复和健康检查。
- 日志文件和运行指标。
- 试点部署文档。

#### 验收门槛

- 真实多边形边界判断不依赖外接矩形。
- 规则结果可解释并包含数据来源和版本。
- 新服务器可在30分钟内完成安装。
- 删除或损坏演示数据库后可恢复。
- 服务异常重启后数据不丢失。

### Sprint 6：设备适配与动态运行

优先级：中高。

#### P0

- 标准飞行器遥测模型。
- 模拟器和真实设备适配器接口统一。
- 心跳、位置、电量、链路和状态接入。
- WebSocket或消息队列实时刷新。
- 偏航、低电量、失联、临时限制区自动触发规则。
- 备选航线生成。
- 人工确认后继续或终止任务。
- 事件回放和审计时间线。

#### 验收门槛

- 模拟设备和真实设备使用同一任务状态机。
- 链路中断不会直接触发自动飞控动作。
- 所有重规划必须人工确认。
- 事件从产生到处置全过程可回放。

## 5. 技术原则

### 数据原则

- 原始数据、计算结果和人工决策分开保存。
- 所有图层必须有来源、版本、发布时间和可信等级。
- 所有运行结果必须可追溯。
- 真实数据和演示数据必须显式区分。

### 安全原则

- 默认拒绝，按角色授权。
- 硬约束不可通过前端绕过。
- 服务端再次校验所有前端输入。
- 任何重置、发布、停用和人工确认都写审计。
- 不在日志中记录密码、Token或敏感设备凭据。

### 算法原则

- 先保证可解释，再追求复杂度。
- 规则结果必须说明触发规则和建议动作。
- 自动规划只生成建议，不直接控制飞行器。
- 空间算法必须有边界测试和基准数据。

### 交付原则

- 每个 Sprint 保留离线演示路径。
- 每个新功能必须有接口测试。
- 每次发布必须有数据库备份和回滚方案。
- 未通过质量门禁不得进入下一阶段。

## 6. 质量门禁

进入 M1 封闭试点前，必须全部满足：

- [ ] 登录和角色权限通过测试。
- [ ] 关键接口未授权访问全部被拒绝。
- [ ] 数据库迁移可重复执行。
- [ ] PostGIS空间判断通过基准用例。
- [ ] 备份和恢复演练成功。
- [ ] Docker全新环境部署成功。
- [ ] 关键接口测试覆盖率达到80%以上。
- [ ] 连续运行24小时无进程崩溃。
- [ ] 关键操作均可审计。
- [ ] 模拟设备链路可完整回放。

## 7. 明确暂缓事项

在 M1 完成前暂缓：

- 农业作业流程。
- 应急指挥流程。
- 物流配送流程。
- 自动飞行控制。
- 面向公众的开放平台。
- 复杂AI自主决策。
- 过早的三维视觉重构。

## 8. 决策规则

出现以下任一情况，必须暂停功能扩展并优先修复：

- 未授权用户可以执行敏感操作。
- 规则判断无法解释或无法复现。
- 真实数据与演示数据混用。
- 服务重启造成任务或审计丢失。
- 事件处置无法回放。
- 数据库迁移不可回滚。
- 自动动作可能绕过人工确认。

## 9. 下一步执行顺序

立即进入 Sprint 4，严格按以下顺序开发：

1. 用户和角色数据模型。
2. 登录及会话接口。
3. API权限中间件。
4. 审计记录关联操作人。
5. SQLite迁移与PostgreSQL适配接口。
6. 结构化日志和请求ID。
7. Sprint 4安全验收。

只有 Sprint 4 的权限和数据门禁通过，才进入 PostGIS和正式试点部署。
