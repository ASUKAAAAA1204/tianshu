# 梁平低空运行基础底座平台
# 开发执行基线

## 1. 文档定位

- 产品需求基线：[PRD-梁平低空运行基础底座平台.md](./PRD-梁平低空运行基础底座平台.md)
- 本文档版本：V1.0
- 文档状态：开发执行基线
- 适用对象：两人开发团队、测试人员、路演准备人员
- 默认工作目录：`D:\CQUPT\self_file\competition\dkdkdk`

### 1.1 与PRD的关系

- PRD回答：平台做什么、做到什么程度、哪些功能不做。
- 本文档回答：先做什么、如何搭建、如何验收、遇到阻断如何处理。
- 两份文档共同构成开发约束。
- 新需求必须先登记为P1或P2，并说明对P0的影响；未经确认不得改变P0范围。

## 2. 开发总目标

在离线环境下稳定完成以下P0闭环：

```text
数据加载
  → 任务创建
  → 规则校验
  → 主航路/备用航路计算
  → 人工确认
  → 模拟运行
  → 异常注入与告警
  → 人工确认重规划
  → 轨迹回放与报告导出
```

### 2.1 最终必须证明

系统能够回答四个问题：

1. 这个任务能不能飞？
2. 为什么推荐这条航路？
3. 出现异常后怎么办？
4. 整个过程留下了哪些可追溯证据？

## 3. 开发硬约束

1. 先完成P0，再做P1；不以P1功能阻塞P0交付。
2. 首期不做农业、物流、应急、城管等行业专用业务。
3. 不做自动放飞、自动驾驶、自动返航和真实飞行器控制。
4. 平台不替代法定审批、空管和监管系统。
5. 规则硬约束优先于算法优化。
6. 三维效果不能阻塞数据、规则、航路和运行闭环。
7. 真实数据不足时使用明确标记的演示数据，不伪造真实结果。
8. 任何目标指标必须标注“目标值”或“待验证”，不得写成既成事实。
9. 两人都必须能够独立完成核心Demo。
10. 每个完成的功能必须有成功路径、失败路径和验收记录。

## 4. 系统搭建步骤

### Step 1：确认环境和边界

**负责人：A+B**

检查：

- 工作目录和已有代码；
- Node.js、Python及包管理器；
- 是否已有前端、后端、地图或数据资产；
- 本地是否允许安装依赖；
- 是否可以无网络启动。

确认结果写入`docs/environment.md`。

**完成标准：**环境可启动，P0范围已冻结。

### Step 2：建立项目骨架

**负责人：B；A验收**

建议目录：

```text
frontend/
backend/
app/
data/
  source/
  processed/
  demo/
  rules/
  telemetry/
  reports/
docs/
tests/
scripts/
```

完成：

- 前端启动；
- 后端启动；
- 数据库初始化；
- 统一接口返回；
- 统一错误码；
- 日志；
- 一键启动脚本；
- README。

**验收：**浏览器能打开登录页、演示账号能进入总览页、默认Demo数据能加载。

### Step 3：建立最小数据模型

**负责人：B；A定义字段和验收**

实现对象：

- AirspaceCell：空域网格；
- AirspaceRule：空域规则；
- Facility：基础设施；
- Vehicle：飞行器；
- Mission：任务；
- Route：航路；
- Telemetry：遥测；
- Event：事件；
- AuditLog：审计日志。

所有对象必须包含：

- 唯一ID；
- 创建/更新时间；
- 来源；
- 版本；
- 状态。

**验收：**能够新增飞行器、创建任务、加载禁飞区、查询规则版本、写入审计日志。

### Step 4：完成地图和图层

**负责人：B；A提供图层和验收**

P0优先二维或2.5D地图，使用Leaflet或MapLibre。实现：

- 底图；
- 可飞、限飞、禁飞和临时管制区；
- 障碍物；
- 起降点和备降点；
- 通信和导航设施；
- 航路；
- 飞行器位置；
- 图层开关和对象详情。

每个图层显示：来源、时间戳、版本和可信等级。

**验收：**进入总览页后，能够看到验证区域和所有默认图层，点击对象能查看属性。

### Step 5：实现规则引擎

**负责人：B；A编写规则案例**

先实现硬约束：

- 禁飞区；
- 临时管制区；
- 限飞高度；
- 障碍物安全间隔；
- 飞行器续航；
- 飞行器载荷；
- 通信要求；
- 起降/备降设施能力；
- 多任务时间冲突。

再实现软目标：

- 距离；
- 能耗；
- 风险；
- 时效；
- 通信质量。

规则结果必须包含：规则ID、版本、触发对象、具体原因、严重级别和建议。

**验收：**至少通过5组测试：正常、穿越禁飞区、高度超限、续航不足、临时管制后失效。

### Step 6：实现第一版航路计算

**负责人：B；A提供输入和人工基线**

按以下顺序：

1. 验证区域栅格化；
2. 过滤硬约束不可行节点；
3. 使用确定性A*、NetworkX或基础栅格搜索；
4. 计算距离、风险、能耗、时间和通信代价；
5. 输出主航路；
6. 排除主航路后生成备用航路；
7. 返回不可行原因。

综合代价：

`C(P)=λd×距离+λe×能耗+λr×风险+λt×时效代价+λc×通信代价`

首版不宣称算法原创。若后续有算法改进，必须提供基线对照实验。

**验收：**主/备航路不穿越禁飞区，高度和能量满足规则；同输入同版本结果可复现；失败时能解释原因。

### Step 7：增加高度和时间维度

**负责人：B；A验收规则**

顺序不能颠倒：

1. 先完成二维路线；
2. 增加高度层`(x,y,z)`；
3. 增加时间窗口`(x,y,z,t)`；
4. 加入临时管制和多任务时间冲突。

**验收：**系统能判断同一空间位置在不同高度或时间是否可用。

### Step 8：实现运行模拟器

**负责人：B；A编写演示脚本**

使用预置轨迹生成遥测：

- 位置；
- 高度；
- 速度；
- 航向；
- 电量；
- 链路质量；
- 当前航段；
- 时间戳。

模拟器只改变平台状态，不发送真实飞控指令。

**验收：**点击开始后，飞行器沿航路移动，状态持续刷新，任务状态正确变化。

### Step 9：实现异常注入和告警

**负责人：B；A定义等级和处置话术**

首批事件：

1. 临时管制；
2. 偏航；
3. 低电量；
4. 链路下降。

处理链：

```text
发现 → 分级 → 生成建议 → 人工确认 → 模拟重规划 → 写入审计
```

**验收：**事件在地图、告警列表和任务详情同步显示；告警可关联到任务和轨迹；确认结果写入日志。

### Step 10：实现重规划和人工确认

**负责人：B；A验收交互和安全边界**

实现：

- 标记受影响航段；
- 局部绕行；
- 重新验证硬约束；
- 展示新旧航路差异；
- 用户确认或拒绝；
- 无可行解时提示全局重规划或人工处置。

禁止默认自动确认，禁止向真实无人机下发指令。

**验收：**重规划结果通过所有硬约束，且保存确认人、时间、原因和新航路版本。

### Step 11：实现轨迹回放、报告和审计

**负责人：B；A负责报告内容**

报告必须包括：

- 任务信息；
- 飞行器信息；
- 数据版本；
- 规则版本；
- 计划航路；
- 实际轨迹；
- 告警和处置；
- 重规划前后差异；
- 人工确认；
- 最终状态。

P0导出HTML/PDF和JSON。

**验收：**按任务可回放全过程，报告可导出，关键操作可在审计日志查询。

### Step 12：完成P0验收和离线封装

**负责人：A验收；B修复和打包**

完成：

- 功能测试；
- 算法测试；
- 异常测试；
- 断网测试；
- 数据版本测试；
- 规则版本测试；
- 权限测试；
- 连续三次Demo测试。

**发布条件：**完整Demo连续三次至少成功两次；网络断开后仍能使用预置数据演示；所有P0验收项有记录。

### Step 13：最后做视觉和P1增强

只有P0发布后，才允许开发：

- Cesium三维地形和建筑；
- 高质量动画；
- 真实无人机遥测；
- 历史数据风险校准；
- AI异常模式识别；
- 更复杂的多机调度。

任何P1功能不得破坏P0接口、数据版本和演示流程。

## 5. 两人开发分工

### 5.1 成员A：产品、业务与验收

- 维护PRD和范围；
- 定义数据字段；
- 整理梁平本地化资料；
- 编写规则案例；
- 准备测试用例；
- 验收页面和接口；
- 编写Demo脚本、PPT和答辩材料；
- 管理数据来源、许可和证据；
- 负责对外沟通。

### 5.2 成员B：技术、算法与交付

- 搭建前后端；
- 建立数据库和数据导入；
- 实现地图和图层；
- 实现规则引擎；
- 实现航路计算；
- 实现模拟器、事件和重规划；
- 实现回放、报告和日志；
- 编写测试、启动和离线部署脚本。

### 5.3 共同责任

- 两人每日同步15分钟；
- 每个Sprint结束必须有可运行版本；
- 两人都能独立完成核心Demo；
- 成员A审查技术表述，成员B审查业务可行性；
- 所有阻断当天登记，不允许隐瞒到最后。

## 6. 开发顺序与阶段验收

### Sprint 0：项目骨架

交付：环境说明、目录、启动脚本、登录、基础布局、Demo数据。

### Sprint 1：数据与地图

交付：图层导入/发布、地图展示、空域网格、设施和飞行器对象。

### Sprint 2：规则与航路

交付：硬约束、软目标、主航路、备用航路、不可行原因、冲突检测。

### Sprint 3：运行模拟

交付：遥测、运行监视、异常注入、告警、人工确认和重规划。

### Sprint 4：审计与报告

交付：轨迹回放、报告导出、日志查询和版本关联。

### Sprint 5：比赛封装

交付：离线包、视频、截图、测试报告、5分钟Demo脚本和故障预案。

## 7. 阻断处理机制

### 7.1 数据阻断

**问题：**梁平真实数据无法及时获得。

**处理：**先使用公开或仿真数据，页面和报告标注“演示数据”；数据层采用统一格式，真实数据到位后替换数据文件，不重写规则和算法。

**不得：**将仿真结果写成梁平真实运行结果。

### 7.2 三维地图阻断

**降级顺序：**三维 → 2.5D → 二维 + 高度剖面 → 预渲染图片/视频。

**原则：**地图效果不能阻塞规则、航路和运行闭环。

### 7.3 算法阻断

**降级顺序：**复杂搜索 → 基础A* → 二维栅格搜索 → 预置可行航路。

无论降级到哪一层，都必须保留硬约束校验和不可行原因。

### 7.4 真实设备阻断

无设备、无飞手或无安全试飞条件时，使用模拟遥测完成P0。材料中明确“本版本验证运行底座模拟闭环”。

### 7.5 外部接口阻断

所有外部服务通过Adapter适配层接入。接口不可用时切换Mock数据，不能让外部服务成为P0单点依赖。

### 7.6 进度阻断

- 落后1天：删除动画、复杂统计和非核心筛选；
- 落后3天：删除三维地形、多机复杂调度、AI异常识别和真实设备接入；
- 落后5天：只保留数据加载、任务创建、规则校验、单航路、异常注入和报告导出。

### 7.7 需求阻断

新增需求必须回答：

1. 是否属于P0？
2. 是否直接支持主闭环？
3. 是否影响当前截止时间？

任一答案不利，自动转入P1/P2。

## 8. 统一开发规范

- 代码版本：`v0.x-feature`、`v1.0-demo`；
- 数据目录：`source`、`processed`、`demo`分离；
- 规则、数据、算法配置均有版本号；
- 所有写接口记录操作者和请求ID；
- 所有异常有统一错误码；
- 关键功能至少有一个自动化或手工测试；
- 代码、数据、PPT和视频至少保留三份备份；
- 不把敏感数据提交到公开仓库；
- 对外文案不得夸大模拟结果。

## 9. Definition of Done

功能只有同时满足以下条件才算完成：

1. 页面或接口可运行；
2. 正常和失败路径均有处理；
3. 关键操作有日志；
4. 至少有一个验收用例；
5. 数据、规则和算法版本可追溯；
6. 成员A能从业务角度讲清楚；
7. 成员B能从技术角度解释；
8. 不违反PRD范围和安全边界；
9. 已在离线环境验证。

## 10. 最终发布标准

平台达到以下状态才允许作为比赛正式版本：

- 能显示验证区域和空域/设施图层；
- 能创建通用任务；
- 能执行硬约束校验；
- 能生成主航路和备用航路或解释不可行原因；
- 能模拟运行；
- 能注入临时管制、偏航、低电量和链路下降；
- 能人工确认重规划；
- 能回放轨迹和导出报告；
- 能查询规则、数据、任务和事件日志；
- 能离线启动；
- 两人都能完成5分钟Demo；
- 所有展示结果都能说明来源、版本和是否为演示数据。

